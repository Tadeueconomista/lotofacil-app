<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Sorteio Inteligente</title>
  <link rel="icon" href="LOTOFÁCIL.png" sizes="192x192" type="image/png">
  <link rel="icon" href="LOTOFÁCIL.png" sizes="512x512" type="image/png">
  <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
  <meta http-equiv="Pragma" content="no-cache" />
  <meta http-equiv="Expires" content="0" />
  <style>
    * { box-sizing: border-box; }
    html, body {
      margin: 0;
      padding: 0;
      background-color: #202020;
      color: #ffffff;
      font-family: Arial, sans-serif;
      text-align: center;
      overflow-x: hidden;
    }
    #splash {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: #121212;
      color: #00ffd5;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      z-index: 9999;
      font-size: 24px;
      animation: fadeOut 1.2s ease-in-out 2s forwards;
    }
    @keyframes fadeOut {
      to { opacity: 0; visibility: hidden; }
    }
    .container {
      padding: 20px;
      max-width: 480px;
      margin: 0 auto;
    }
    h1 {
      font-size: 22px;
      margin-bottom: 20px;
      color: #00ffd5;
    }
    .inputs {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 8px;
      margin-bottom: 20px;
    }
    input[type="number"] {
      width: 100%;
      padding: 8px;
      border-radius: 6px;
      border: none;
      background-color: #303030;
      color: #ffffff;
      font-size: 15px;
      box-shadow: 0 0 5px #00ffd5;
      text-align: center;
    }
    .buttons {
      margin-bottom: 10px;
    }
    button {
      background-color: #00ffd5;
      color: #121212;
      border: none;
      padding: 10px 20px;
      border-radius: 8px;
      font-weight: bold;
      font-size: 14px;
      cursor: pointer;
      margin: 5px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    button:active {
      transform: scale(0.95);
      box-shadow: 0 0 10px #00ff80;
    }
    #statusImportacao {
      margin-bottom: 10px;
      color: #00ffd5;
      font-weight: bold;
      font-size: 15px;
    }
    .resultado {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: 8px;
      margin-top: 10px;
    }
    .label {
      background-color: #303030;
      padding: 10px;
      border-radius: 8px;
      box-shadow: 0 0 5px #00ffd5;
      font-size: 16px;
      color: #00ffd5;
    }
    .sorteado {
      background-color: #00ff80;
      color: #121212;
      font-weight: bold;
      box-shadow: 0 0 5px #00ff80;
      animation: piscar 0.6s ease-out;
    }
    @keyframes piscar {
      0% { opacity: 0; transform: scale(0.8); }
      50% { opacity: 1; transform: scale(1.05); }
      100% { opacity: 1; transform: scale(1); }
    }
  </style>
</head>
<body>
  <img src="lotofacil.php" style="display:none;">
  <div id="splash">
    <h2>Sorteio Inteligente</h2>
    <p>Bem-vindo!</p>
  </div>
  <div class="container">
    <h1>Digite até 15 números de 1 a 25</h1>
    <div class="inputs" id="entrada">
      <input type="number" /><input type="number" /><input type="number" /><input type="number" /><input type="number" />
      <input type="number" /><input type="number" /><input type="number" /><input type="number" /><input type="number" />
      <input type="number" /><input type="number" /><input type="number" /><input type="number" /><input type="number" />
    </div>

    <div class="buttons">
      <button onclick="sortearComLogica()">Sortear</button>
      <button onclick="limparTudo()">Limpar</button>
      <button onclick="importarUltimos()" id="botaoImportar">Importar últimos resultados</button>
      <button onclick="sortearComFrequencia()">Probabilidade histórica</button>
      <button onclick="carregarCombinacoesReais()">Combinação real</button>
      <button onclick="atualizarConcursos()">Atualizar histórico</button>
      <button id="btnPromissor" onclick="prepararPromissor()">🎯 Jogos Promissores</button>
      <button onclick="gerarJogosPromissores(10)">📈 Estratégias Otimizadas</button>
    </div>

    <div style="margin-bottom: 20px;">
      <input type="number" id="numeroConcurso" placeholder="Digite o nº do concurso"
        style="padding: 8px; border-radius: 6px; width: 180px; background-color: #303030; color: #ffffff; box-shadow: 0 0 5px #00ffd5; border: none; text-align: center;">
      <button onclick="buscarConcursoEspecifico()" style="margin-left: 10px;">Buscar concurso</button>
    </div>

    <div id="statusImportacao"></div>

    <!-- ✅ NOVA SEÇÃO: Números Fixos Superiores -->
    <h2 style="color: #00ffd5;">🔒 Números Fixos</h2>
    <div id="fixos" class="resultado"></div>
    <div class="buttons">
      <button onclick="inserirFixosManual()">Digitar Números Fixos</button>
      <button onclick="sortearFixos()">Sortear Números Fixos</button>
      <button onclick="apagarFixos()">Apagar Números Fixos</button>
    </div>

    <!-- ✅ NOVA SEÇÃO: Números de Comparação Inferiores -->
    <h2 style="color: #00ffd5;">🔍 Comparação</h2>
    <div id="comparacao" class="resultado"></div>
    <div class="buttons">
      <button onclick="sortearComparacao()">Sortear Comparação</button>
    </div>

    <!-- Continuação original -->
    <div class="resultado" id="resultado"></div>
    <div id="jogosGerados"></div>
    <div id="analiseEstatistica"></div>
<script>
  // ✅ NÚMEROS FIXOS — VARIÁVEIS E FUNÇÕES
  let numerosFixos = [];
  let numerosFixosBloqueados = false;

  function mostrarNumeros(id, numeros) {
    const container = document.getElementById(id);
    container.innerHTML = "";
    numeros.forEach(n => {
      const label = document.createElement("div");
      label.className = "label";
      label.textContent = n.toString().padStart(2, "0");
      container.appendChild(label);
    });
  }

  function sortearFixos() {
    if (numerosFixosBloqueados) return;
    numerosFixos = gerarNumerosAleatorios();
    mostrarNumeros("fixos", numerosFixos);
    numerosFixosBloqueados = true;
  }

  function inserirFixosManual() {
    if (numerosFixosBloqueados) return;
    const entrada = prompt("Digite 15 números separados por vírgula:");
    const lista = entrada
      .split(",")
      .map(n => parseInt(n.trim()))
      .filter(n => !isNaN(n) && n >= 1 && n <= 25);

    if (lista.length === 15) {
      numerosFixos = lista;
      mostrarNumeros("fixos", numerosFixos);
      numerosFixosBloqueados = true;
    } else {
      alert("Você precisa digitar exatamente 15 números de 1 a 25.");
    }
  }

  function apagarFixos() {
    numerosFixos = [];
    numerosFixosBloqueados = false;
    document.getElementById("fixos").innerHTML = "";
  }

  function sortearComparacao() {
    const numeros = gerarNumerosAleatorios();
    mostrarNumeros("comparacao", numeros);
  }

  function gerarNumerosAleatorios() {
    const sorteados = [];
    while (sorteados.length < 15) {
      let n = Math.floor(Math.random() * 25) + 1;
      if (!sorteados.includes(n)) sorteados.push(n);
    }
    return sorteados.sort((a, b) => a - b);
  }

  // 👇 Continua com seus scripts originais...
</script>
</body>
</html>

